#!/bin/bash
set -euo pipefail

# Fake SwitchPointServer "use_aws" script for local development.

STATE_FILE="${SWITCHPOINT_STATE_FILE:-/tmp/switchpoint_mode}"
echo "aws" > "$STATE_FILE"
echo "✅ [FAKE] Switched to AWS mode."


